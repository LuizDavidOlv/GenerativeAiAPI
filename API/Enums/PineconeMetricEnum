from enum import Enum
class PineConeMetricEnum(str, Enum):
    euclidean = "euclidean"
    cosine = "cosine"
    inner_product = "inner_product"

    def __str__(self):
        return str(self.value)
    
    def __repr__(self):
        return str(self.value)